import { TouchScreenOrDirective } from './touch-screen-or.directive';

describe('TouchScreenOrDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchScreenOrDirective();
    expect(directive).toBeTruthy();
  });
});
