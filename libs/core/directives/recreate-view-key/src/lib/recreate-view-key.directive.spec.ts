import { RecreateViewKeyDirective } from './recreate-view-key.directive';

describe('RecreateViewKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new RecreateViewKeyDirective();
    expect(directive).toBeTruthy();
  });
});
