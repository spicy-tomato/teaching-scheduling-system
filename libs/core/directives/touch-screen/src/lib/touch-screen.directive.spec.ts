import { TouchScreenDirective } from './touch-screen.directive';

describe('TouchScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchScreenDirective();
    expect(directive).toBeTruthy();
  });
});
