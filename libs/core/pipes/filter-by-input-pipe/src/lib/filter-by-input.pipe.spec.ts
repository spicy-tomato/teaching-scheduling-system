import { FilterByInputPipe } from './filter-by-input.pipe';

describe('FilterByInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByInputPipe();
    expect(pipe).toBeTruthy();
  });
});
