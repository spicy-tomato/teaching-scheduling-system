import { NavbarNamePipe } from './navbar-name.pipe';

describe('NavbarNamePipe', () => {
  it('create an instance', () => {
    const pipe = new NavbarNamePipe();
    expect(pipe).toBeTruthy();
  });
});
