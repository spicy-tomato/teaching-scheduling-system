import { TeachingHistoryDirective } from './teaching-history.directive';

describe('TeachingHistoryDirective', () => {
  it('should create an instance', () => {
    const directive = new TeachingHistoryDirective();
    expect(directive).toBeTruthy();
  });
});
