import { ShiftPipePipe } from './shift-pipe.pipe';

describe('ShiftPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftPipePipe();
    expect(pipe).toBeTruthy();
  });
});
