import { CalendarDateTimePipe } from './calendar-date-time.pipe';

describe('CalendarDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
