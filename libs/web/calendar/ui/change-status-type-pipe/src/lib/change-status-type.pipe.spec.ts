import { ChangeStatusTypePipe } from './change-status-type.pipe';

describe('ChangeStatusTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeStatusTypePipe();
    expect(pipe).toBeTruthy();
  });
});
