import { NavigateDirective } from './navigate.directive';

describe('NavigateDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigateDirective();
    expect(directive).toBeTruthy();
  });
});
