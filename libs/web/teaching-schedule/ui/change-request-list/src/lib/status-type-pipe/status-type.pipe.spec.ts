import { StatusTypePipe } from './status-type.pipe';

describe('StatusTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTypePipe();
    expect(pipe).toBeTruthy();
  });
});
