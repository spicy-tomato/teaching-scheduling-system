import { CanExportPipe } from './can-export.pipe';

describe('CanExportPipe', () => {
  it('create an instance', () => {
    const pipe = new CanExportPipe();
    expect(pipe).toBeTruthy();
  });
});
